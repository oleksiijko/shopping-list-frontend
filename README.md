# Shopping List Frontend

React + TypeScript + Vite frontend for the Shopping List app.

## Requirements

- Node.js 18+
- npm

## Install

```bash
npm install
```

## Run (development)

```bash
npm run dev
```

Frontend runs on `http://localhost:5173`.
By default, API requests to `/api` are proxied to `http://localhost:3000`.

## Scripts

- `npm run dev` — start dev server
- `npm run test` — run tests
- `npm run test:watch` — run tests in watch mode
- `npm run lint` — run ESLint
- `npm run build` — type-check and production build
- `npm run preview` — preview production build

## Backend

Run backend separately from:

`/Users/olebonda/Desktop/tack/shopping-list-api`

Expected API base path: `/api`.
